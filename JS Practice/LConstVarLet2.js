let age =30;
console.log(age);

age=31;
console.log(age);
// we re-assign number of values to let and
//we can leave it without assigning a value also

const birthYear=2001;
// we cannot re-assign new values to const keyword and 
//always we need to assign a value to it

var job='programmer';
console.log(job);
job='developer';
console.log(job);

// we can also use variables without declaring
lastName='Koneti';
console.log(lastName);